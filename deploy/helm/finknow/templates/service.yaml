apiVersion: v1
kind: Service
metadata:
  name: {{ include "finknow.fullname" . }}
  labels:
    {{- include "finknow.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "finknow.selectorLabels" . | nindent 4 }}
